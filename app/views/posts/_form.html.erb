<div class="form_wrapper">
	<%= form_for @post do |f| %>
		<%= f.label :title, class: 'text'%><br>
		<%= f.text_field :title %><br>
		<br>
		<%= f.label :body, class: 'text' %><br>
		<%= f.text_area :body %><br>
		<br>
		<%= f.submit %>
	<% end %>
</div>
